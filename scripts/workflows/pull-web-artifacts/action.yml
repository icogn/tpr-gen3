---
name: 'Pull web artifacts'
author: 'icogn'
description: 'Action to verify and add web artifacts to branch for use by offline generator'

branding:
  icon: 'plus'
  color: 'gray-dark'

inputs:
  allowedWebBranches:
    description: Allowed web branches
    required: true

  artifactInfo:
    description: Artifact info
    required: true

# outputs:
#   value:
#     description: The version

runs:
  using: node20
  main: pull-web-artifacts.js
